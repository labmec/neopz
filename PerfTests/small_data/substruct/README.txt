The Perf-Substruct application was designed to measure
the performance of the SubStruct application.

inputs: contain the input files. Including some checkpoints to allow skipping
        some of the steps.

outputs: contains the golden outputs. They should be used to verify if the
         output of the execution is correct.

