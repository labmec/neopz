# @file neopz/UnitTest_PZ/TestMatrixComplex/CMakeLists.txt  -- CMake file for unit test of the complex matrices

if(STATE_COMPLEX) 
	file(GLOB headers *.h)
	file(GLOB sources *.cpp)

else()

endif()

